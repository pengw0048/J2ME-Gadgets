在手机上画函数图像，不限屏幕大小。最多同时显示10个函数。有三种模式：多项式、指令和表达式模式。
多项式：按照系数和次数输入多项式表达式
指令：选择两个操作数和运算符，结果可以存入y或者临时变量中（下标0-19）
表达式：按照basic语法的算术表达式，例如-sqrt(abs(sin(x*3.14-2)+x^int(x))。支持的函数有sqrt()、abs()、int()、sin()、cos()、tan()、ln()。^是乘方
然后在显示函数的界面，按确定键在平移、缩放、鼠标和设置中切换。鼠标工具很智能。
它有什么用吗？没有。